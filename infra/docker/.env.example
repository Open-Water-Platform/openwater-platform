# Local-dev defaults for the Postgres container in docker-compose.yml.
# Copy this file to `.env` in the same directory (the real `.env` is
# gitignored). These credentials are intentionally trivial and must never
# be reused in any non-local environment.

POSTGRES_USER=owp
POSTGRES_PASSWORD=owp
POSTGRES_DB=owp
